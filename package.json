{
  "name": "etendo-lite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"server,ui\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix ui\"",
    "install:all": "npm install --prefix server && npm install --prefix ui"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
